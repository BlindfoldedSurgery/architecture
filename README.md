# BlindfoldedSurgery Architecture Docs

## Local Usage

Either install the Python dependencies locally and use mkdocs regularly, or run
`docker-compose up` and use the live view at <http://localhost:8000>.
